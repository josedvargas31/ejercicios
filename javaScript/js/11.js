// Ejercicio &: Raiz cuadrada de un número
// Escribe una función llamada "calcularRaiz" que tome un número como parámetroy devuelva su raiz.
// Ejemplo: sumar(25) debería devolver 5.
let numero = 25;
function calcularRaiz(data) {
    // let number = 0;
     return Math.sqrt(data);
}
let resultado = calcularRaiz(numero);
console.log(resultado);